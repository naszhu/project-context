    
       ///****************************************************
    //                    add testpos corrsponding for backward condition, 
    // this is generated by chatGPT with my description.....
    //****************************************************
   for (let i = 0; i < tot_word_arobj_nf_rd.length; i += 2) {
        const currentArray = tot_word_arobj_nf_rd[i];
        const nextArray = tot_word_arobj_nf_rd[i + 1];

        // Iterate through the objects in the current array
        currentArray.forEach(currentObj => {
            // console.log(currentObj)
            // Find the corresponding object in the previous array
            if (currentObj.wordcondi === 'repeat' && condi=="b") {
                // console.log("ssss",currentObj)
                // Find the corresponding object in the previous array
                const associatedObj = nextArray.find(
                prevObj => prevObj.ftl === currentObj.ftl
                );
                
                // Assign the id from the associated object to the current object
                if (associatedObj) {
                    currentObj.testpos_final = associatedObj.testpos_final;
                }
            }
        });
    }